<div class="content">
    <h1>{{ content.title }}</h1>
    <p>{{ content.url }}</p>
    {% for block in content.block %}
        {% if block.type == 'text' %}
            <h2>{{ block.headline }}</h2>
            <p>{{ block.text|raw }}</p>
        {% endif %}
    {% endfor %}
</div>